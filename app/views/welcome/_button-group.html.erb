<div class="button-container">
  <% people.each do |person| %>
    <div class='button bio-button' data-target="<%= person[:class] %>"><%= person[:name] %></div>

    <%= render partial: 'bio', locals: { person: person } %>
  <% end %>
</div>
